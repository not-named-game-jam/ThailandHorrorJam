using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InEndingtrue : MonoBehaviour
{
    public bool InEnding;
    // Start is called before the first frame update
    void Start()
    {
        InEnding = true;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
